import { LocationButton, MapView, SearchBar } from '../components';

export const HomeScreen = () => {
  return (
    <div>
      <MapView/>
      <LocationButton/>
      <SearchBar/>
    </div>
  )
}
